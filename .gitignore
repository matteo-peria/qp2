config/ifort.cfg
quantum_package_static.tar.gz
build.ninja
.ninja_log
.ninja_deps
bin/
lib/
lib64/
libexec/
config/qp_create_ninja.pickle
src/*/.gitignore
ezfio_interface.irp.f
share
*.swp
# git submodules
/external/ezfio/
/external/irpf90/
# Other files produced by compilation
etc/00.qp_root.rc
# Ignore everything inside plugins
/plugins/*
# Except changes to default local plugins and README file
!/plugins/local/
!/plugins/README.rst
!/plugins/.gitignore
## Ignore everything inside each plugin directory 
##(links to other directories, executables, automatic Makefiles)
#/plugins/*/*
## Except changes to code
#!/plugins/*/*.irp.f
#!/plugins/*/*.f90
#!/plugins/*/*.F90
#!/plugins/*/NEED
#
# ignore everything in plugin/local directories
/plugins/local/*/*
# except files irp.f files, but ignore the automatic ezfio_interface
#!/plugins/local/*/*.*
!/plugins/local/*/*.irp.f

/plugins/local/*/ezfio_interface.irp.f
# do not ignore local plugins files
!/plugins/local/*/*.f90
!/plugins/local/*/*.F90
!/plugins/local/*/NEED
